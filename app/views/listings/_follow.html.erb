<%= form_for(current_user.active_relationships.build(listing_id: @listing.id),
																							remote: true) do |f| %>
  <div><%= hidden_field_tag :listing_id, @listing.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>